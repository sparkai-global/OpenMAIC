{
  "id": "openmaic",
  "name": "OpenMAIC",
  "description": "Manage and use OpenMAIC multi-agent interactive classroom",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "default": "http://localhost:3000",
        "description": "OpenMAIC server URL"
      },
      "projectDir": {
        "type": "string",
        "description": "Path to OpenMAIC project directory (for docker compose commands)"
      }
    }
  }
}
